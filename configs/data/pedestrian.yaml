_target_: src.datasets.pedestrian.PedestrianDataModule
first_stage: ${first_stage}
scenes: [zara1, zara2, univ, hotel, eth]
past_frames: 8
future_frames: 12
traj_scale: 1
return_index: True
rand_rotation: True
rand_translation: 1
flip_vertical: False
flip_horizontal: False
shift: ${shift}
scale: ${scale}
num_entities: ${num_entities}
batch_size: 1024
drop_last: True
num_workers: 31
pin_memory: True
persistent_workers: True
prefetch_factor: 2
